import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.kachuful.com',
  appName: 'Kachuful',
  webDir: 'dist',
  backgroundColor: '#0f172acc'
};

export default config;
